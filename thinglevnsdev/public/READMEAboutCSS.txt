The css/style.css file is generate using from .scss files


To regnerate install all the SASS related gem for instance currently I have:

bootstrap-sass (2.0.3.1)
compass (0.12.1)
sass (3.1.19)

the go to the public directly and run "compass compile -f"

